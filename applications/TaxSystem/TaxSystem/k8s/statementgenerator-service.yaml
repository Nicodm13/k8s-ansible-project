apiVersion: apps/v1
kind: Deployment
metadata:
  name: statementgenerator-service
  labels:
    app: statementgenerator-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: statementgenerator-service
  template:
    metadata:
      labels:
        app: statementgenerator-service
    spec:
      containers:
        - name: statementgenerator-service
          image: taxsystem-statementgenerator-service:latest
          imagePullPolicy: Never
          env:
            - name: RABBITMQ_HOST
              value: "rabbitmq"
            - name: RABBITMQ_PORT
              value: "5672"
---
apiVersion: v1
kind: Service
metadata:
  name: statementgenerator-service
spec:
  selector:
    app: statementgenerator-service
  ports:
    - port: 80
      targetPort: 8080

